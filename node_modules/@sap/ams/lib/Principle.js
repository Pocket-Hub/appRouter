class Principle {
    constructor(tenant, userId) {
        this.tenant = tenant;
        this.userId = userId;
    }

    toCacheKey() {
        return [this.tenant, this.userId]
        .map(s => `${s.length}:${s}`).join(":");
    }
}

module.exports = Principle;