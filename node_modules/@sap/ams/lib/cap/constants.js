module.exports = {
    CAP_PSEUDO_ROLES : ["any", "authenticated-user", "internal-user", "system-user"],
    CAP_USER_ROLE_ACTIVE : 1, // CAP uses 1 instead of true to signal that user has a role,
    RESTRICT_FILTER_ALL : [] // restrictions result that filters away all entites
}