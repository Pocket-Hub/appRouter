export = AmsBuildPlugin;
/** Custom CAP build plugin for AMS to generate DCL policies from the CDS model. */
declare class AmsBuildPlugin {
    static hasTask(): any;
    init(): void;
    clean(): Promise<void>;
    build(): Promise<void>;
}
